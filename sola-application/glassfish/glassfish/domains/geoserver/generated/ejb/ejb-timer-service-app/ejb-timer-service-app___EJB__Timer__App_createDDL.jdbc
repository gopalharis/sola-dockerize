CREATE TABLE "EJB__TIMER__TBL" ("TIMERID" VARCHAR(255) NOT NULL, "APPLICATIONID" BIGINT, "BLOB" BLOB(2147483647), "CONTAINERID" BIGINT, "CREATIONTIMERAW" BIGINT, "INITIALEXPIRATIONRAW" BIGINT, "INTERVALDURATION" BIGINT, "LASTEXPIRATIONRAW" BIGINT, "OWNERID" VARCHAR(255), "PKHASHCODE" INTEGER, "SCHEDULE" VARCHAR(255), "STATE" INTEGER, PRIMARY KEY ("TIMERID"))
